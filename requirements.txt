# Dashboard runtime (also used by Streamlit Cloud builds).
# Full pipeline/ML toolchain lives in requirements-full.txt.

# Core
streamlit[auth]>=1.54.0
pandas>=2.0.0
requests>=2.31.0

# Maps
folium>=0.14.0
streamlit-folium>=0.15.0

# Excel export
openpyxl>=3.1.0
